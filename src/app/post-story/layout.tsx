export default function PostStoryLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  // This layout can be expanded later if the post-story page needs a different frame
  return <>{children}</>;
}
